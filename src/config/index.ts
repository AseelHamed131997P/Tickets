import { Config } from "types/config";

const REACT_APP_GATEWAY = "https://test-portal.rahhaltours.com/api";
const config: Config = {
  api: {
    endpoint: REACT_APP_GATEWAY,
  },
};

export default config;
